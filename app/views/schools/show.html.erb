<h3><%= @school.name %></h3>
<div class="container">
  <div class="wrap">
    <h4>Groups:</h4>
    <ul class="list">
      <% @school.groups.each do |group| %>
        <li><%= link_to group.name+' ('+group.schedules.count.to_s+')', school_group_path(@school,group) %></li>
      <% end %>
    <ul>
  </div>
</div>